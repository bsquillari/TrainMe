<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trainme">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrainMeLogin"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".PlayRoutine"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:label="@string/title_activity_play_routine"
            android:theme="@style/Theme.TrainMeLogin.Fullscreen"
            android:parentActivityName=".DetailRoutine"/>
        <activity
            android:name=".DetailRoutine"
            android:exported="false"
            android:label="@string/title_activity_detail_routine"
            android:theme="@style/Theme.TrainMeLogin.NoActionBar"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".EditProfileActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>

                <!-- Lo que se va abrir -->
                <action android:name="android.intent.action.MAIN" />
                <!-- El iconito en el launcher de apps -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>